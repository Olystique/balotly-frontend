export default function Home() {
  return (
    <main className="mx-auto flex w-full max-w-voter flex-1 flex-col justify-center gap-3 px-4 py-12">
      <h1 className="text-3xl">Balotly</h1>
      <p className="text-base text-muted">
        Paid campus voting with a live, verified count. Voting pages and
        dashboards land here issue by issue.
      </p>
    </main>
  );
}
